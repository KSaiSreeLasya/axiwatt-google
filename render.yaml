services:
  - type: web
    name: axiwatt-solar
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.22.0
    healthCheckPath: /
    autoDeploy: true
